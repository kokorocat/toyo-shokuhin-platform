# Cursor プロンプト: UI/UX改善タスク(第3弾)

以下をそのままCursorのチャットに貼り付けてください。

---

## プロジェクト概要

日本の食品会社グループ(東洋食品/昭和食品/大阪惣菜、3社共同利用)向けの社内業務システム。Next.js 16 (App Router, TypeScript) + Supabase (PostgreSQL, Auth, RLS) + Tailwind CSS v4 + Vercelでホスティングされている。実際に稼働中の本番環境で、リアルな従業員データ・店舗データが入っている。

リポジトリ: このディレクトリ全体(Next.jsプロジェクトルート)

**これは3回目のUI/UX改善パスです。** 作業を始める前に、まず `CURSOR_BRIEF.md`(第2弾のブリーフ)を全文読んでください。第2弾で定義された共有コンポーネント(`src/components/Banner.tsx` / `PageHeader.tsx` / `EmptyState.tsx` / `SubmitButton.tsx`)・配色規約・コンテナ幅規約は今回もそのまま踏襲します。

**今回のブリーフの前提が第2弾と違う点**: 実際にコードを1ファイルずつ確認したところ、受発注・HACCP管理者・レシピ閲覧の3システムとも、第2弾が指摘していた「管理画面のテーブルが素朴」「pending状態表示がない」といった課題の大部分は**既に対応済み**であることが判明しました。したがって今回は全面的な磨き直しではなく、実際にコードを読んで確認した**具体的な残課題のみ**に絞って指示します(下記システム別セクション)。的外れな指示で無関係な箇所を書き換えることのないよう、指定したファイル・箇所以外は変更しないでください。

## 今回のタスクの範囲(重要・第2弾と同一)

**バックエンド・データ層・ビジネスロジックは一切変更しないでください。** 具体的に触ってはいけないもの:

- `supabase/migrations/` 配下のSQL(スキーマ・RLSポリシー)
- 各 `actions.ts` / `route.ts` 内のデータ検証・DB操作ロジック(バリデーション条件、フォームの `name` 属性、insert/update先のテーブル・カラム、注文ステータスの遷移可否判定 `ALLOWED_TRANSITIONS` 等は変更不可)
- `src/lib/portal/get-portal-context.ts`、`src/lib/supabase/`、`src/lib/haccp/admin-dashboard.ts`、`src/lib/date.ts` などの認証・権限・集計ロジック
- 各 `guard.ts`(`isHaccpAdminRole` / `isOrderingAdminRole` / `isRecipeAdminRole` 等)

`src/lib/date.ts` の `todayInTokyo()` に関する既知のバグ(UTC基準の日付ズレ)の教訓は今回も有効です。UI側で日付を扱う新規コードに `new Date().toISOString()` 系のパターンを持ち込まないでください。

変更してよいもの: 見た目(Tailwindクラス、レイアウト)、UIの状態管理(ローディング表示等)、**コンポーネント構成の整理**(下記で指示する「共有コンポーネント化」は、既存の表示ロジックを1箇所にまとめるだけの変更であり、バリデーション・DB操作には一切触れません)。

## 背景: 参考にした外部システム

クライアントから「モダンデザイン」の参考として3システムが共有されました。参考の具体性がシステムごとに大きく異なるため、以下ではその差を踏まえた指示になっています。

- **受発注システム**: CO-NECT(実際の操作マニュアル画像・約50枚を確認済み。具体的なUIパターンを下記で指示)
- **HACCP**: カミナシ(https://kaminashi.jp/ のトップページ紹介文のみ確認。クライアント自身も「UIはあまり参考にしていない、概要のみ参考」とのことで、実際の管理画面キャプチャは今回も未確認のまま)
- **レシピ閲覧システム**: Teach me biz(クライアントが現行使用中の実際の画面キャプチャ(フォルダ一覧・承認ワークフロー設定・マニュアル一覧など)を追加入手。具体的なUIパターンを下記C-3で指示)

---

## A. 受発注システム(`/ordering`)— 最優先

### A-1. 状態バッジの色統一(必須・最初に着手)

以下は実際にコードを読んで確認した、同じ注文ステータスコード(`new` / `in_production` / `preparing_shipment` / `shipped` / `cancelled`)に対して異なる色が使われている実例です:

| ファイル | in_production | preparing_shipment | cancelled |
|---|---|---|---|
| `src/app/ordering/admin/orders/page.tsx`(STATUS_STYLES) | 青 | amber | 赤 |
| `src/app/ordering/admin/orders/[orderId]/page.tsx`(STATUS_BADGE_CLASS) | 青 | amber | 赤 |
| `src/app/ordering/admin/page.tsx`(ORDER_STATUSES) | **amber**(上記2つと不一致) | amber | 赤 |
| `src/app/ordering/history/page.tsx`(STATUS_STYLES) | amber | **紫**(配色規約にない色) | **slate**(他は赤) |

**対応**: `src/lib/ordering/order-status.ts` を新規作成し、`ORDER_STATUS_LABELS` と `ORDER_STATUS_BADGE_CLASS` の2つの定数(または `<OrderStatusBadge status=... />` という表示専用コンポーネント)をそこに1箇所だけ定義してください。これは**表示用のラベル文言とTailwindクラス文字列のマッピングだけ**です。`admin/orders/actions.ts` にある `ALLOWED_TRANSITIONS`(ステータス遷移の可否を判定するビジネスロジック)とは完全に別物なので、そちらには一切触れないでください。

色は `admin/orders/page.tsx` の現行値(new=青, in_production=青, preparing_shipment=amber, shipped=緑, cancelled=赤)を正としてください(4ファイル中2ファイルで既に使われている値のため)。ラベル文言(新規/制作中/出荷準備中/郵送完了/キャンセル)はどのファイルも一致しているため変更不要です。上記4ファイルはこの共有定義をimportするように書き換えてください。

### A-2. 重複マークアップの共通化(ついでに)

以下も見た目を変えずに1箇所にまとめてください:

- 画面下部固定バー(`fixed inset-x-0 bottom-0 z-10 border-t border-slate-200 bg-white/95 px-4 py-3 shadow-[0_-2px_8px_rgba(0,0,0,0.06)] backdrop-blur-sm`)が `CartBar.tsx` / `cart/page.tsx` / `confirm/ConfirmClient.tsx` の3箇所に同一文字列で重複しています。`src/components/StickyActionBar.tsx` として共有化してください。
- 数量+/−ステッパーのマークアップが `ProductRow.tsx` と `cart/page.tsx` に重複しています。`src/app/ordering/QuantityStepper.tsx`(クライアントコンポーネント)として共有化してください。

### A-3. pending状態の追加漏れ(1箇所)

`src/app/ordering/admin/products/page.tsx` の各行「公開する/非表示にする」トグルボタンが、他の管理フォームと違って `SubmitButton` を使わずプレーンな `<button type="submit">` のままです。既存の `SubmitButton` コンポーネントに差し替えてください(他の管理画面フォームと同じ使い方です)。

### A-4. CO-NECT参考: 注文ステータスの進行表示

CO-NECTの受注詳細画面には、注文ステータスの進行を「発注 → 受付中 → 一部出荷済 → 出荷済」という横並びのステップ表示(丸アイコン+日時+担当者名、達成済みは緑チェック)で見せる箇所があります。`src/app/ordering/admin/orders/[orderId]/page.tsx` の「ステータス変更履歴」セクションは既にこのためのデータ(履歴の配列)を持っているので、**新しいクエリやフィールドを追加せず**、既存の履歴配列を横並びステップ形式(丸アイコン+接続線)で見せる表示コンポーネントに置き換えるだけにしてください。データ取得・ロジックは一切変更しないこと。

### A-5. 今回はスコープ外(着手しないでください)

CO-NECTには他にも参考になりそうな機能がありますが、表示ロジックの変更だけでは実現できず、新しいテーブル・カラム・サーバーアクションが必要になるため対象外とします:

- 注文IDごとのメッセージング機能(発注者⇄受注者のやりとりスレッド)
- 納品カレンダー(週/3日/月表示切り替え)
- 取引先・商品のCSV一括インポート

これらは将来的に別途、機能追加として計画します。今回のパスでは触れないでください。

---

## B. HACCP管理者機能(`/haccp/admin`)

### B-1. ステータスバッジの矛盾(必須)

`src/app/haccp/admin/stores/[storeId]/page.tsx` の中で、自主点検の同じ値 `needs_improvement` が、「総合評価」バッジでは赤(`bg-red-100 text-red-700`)、同じページ内の「項目別」バッジでは黄(`bg-amber-100 text-amber-700`)と、**同一ページ内で矛盾**しています。`src/app/haccp/admin/stores/page.tsx` の `inspectionBadge()` は赤を使っているので、これを正として項目別バッジも赤に統一してください。

あわせて `stores/page.tsx` で「未回答/記録なし」に使われている青(`bg-blue-100 text-blue-700`)は、既存の配色規約(緑=成功、赤=要対応、amber=注意、slate=対象外)のどれにも該当しません。ニュートラルな状態なのでslateに変更してください。

### B-2. 既存コンポーネントの再利用

`stores/[storeId]/page.tsx` の「改善が必要な項目の詳細」の注意書きが、既存の `src/components/Banner.tsx` のwarningバリアントとほぼ同じ見た目を手書きで再実装しています(枠線・アイコンなし)。`<Banner variant="warning">` に置き換えてください。

### B-3. 詳細画面の折りたたみ(第2弾から持ち越し・未対応)

`stores/[storeId]/page.tsx` は5セクション・約650行と情報量が多く、現状はセクション分けのみで折りたたみ・ページ内ジャンプナビゲーションがありません。各セクション(重要ポイント/従業員衛生/自主点検/責任者確認/変更履歴)を開閉可能なアコーディオンにするか、ページ上部にセクションへのアンカーリンクを追加してください。

### B-4. カミナシ参考について(限定的・今回は反映不可)

カミナシのトップページからは実際の管理画面キャプチャが確認できず、紹介文レベルの情報(必須入力設定による入力漏れ防止、設備ごとの記録を集約した「カルテ」的な履歴表示、傾向・課題の可視化)しか得られませんでした。このうち「カルテ的な履歴集約」はB-3の折りたたみ対応と方向性が近いですが、それ以外は具体的な指示に落とし込めるだけの情報がありません。**今回はこの項目には着手しないでください。** より詳しい参考が必要な場合は、実際の管理画面のスクリーンショットを追加で入手してから改めて指示します。

---

## C. レシピ閲覧システム(`/recipe`)

このシステムは3システムの中で最も完成度が高く(バッジ配色・pending状態・共有コンポーネント活用いずれも問題なし)、C-1・C-2は小さな統一のみですが、C-3は実際のTeach me biz画面(クライアントより実際の操作画面キャプチャを追加入手済み)を踏まえた新規の具体的な指示です。

### C-1. 「権限がありません」表示の共通化

`submit/page.tsx` / `approvals/page.tsx` / `page.tsx` / `[id]/page.tsx` の4箇所で、同じ「権限がありません」表示が共有コンポーネント化されず手書きされています。`src/components/AccessDenied.tsx`(`message` propを受け取るだけのシンプルな新規コンポーネント)を作成し、4箇所を置き換えてください。

### C-2. モバイルのタップ領域

`[id]/page.tsx` の「関連資料」グリッド内のファイルリンク(`text-xs text-blue-700 hover:underline`)がpaddingなしのテキストリンクのみで、スマートフォンでのタップ領域が狭い可能性があります。`inline-block px-1 py-1` 程度の余白を追加してください。

### C-3. Teach me biz参考: 承認フローの可視化(新規・具体的)

クライアントより、実際に使用しているTeach me bizのフォルダ設定画面のキャプチャを入手しました。「承認ワークフロー」設定タブの下部に、承認フローを4つのアイコン(作成・編集 → 申請 → 承認 → 公開)を矢印でつないだ図解があり、「申請」ノードの上に「承認待ち」の黄色バッジ、「承認」ノードの上に「承認済み」の緑バッジが乗っており、さらに「承認」から「作成・編集」へ戻る「差し戻し」ラベル付きの矢印が下側にループしています。

これは `src/app/recipe/admin/approvals/page.tsx` が既に持っているデータ(下書き/申請中/承認待ち/差し戻し/公開済みという状態遷移、`rejection_note` を含む差し戻し理由)をそのまま可視化に使えます。**新しいクエリ・フィールドは一切追加せず**、承認待ち一覧セクションの上部(または各カードのヘッダー部分)に、この4ノード+差し戻しループの図解を静的な表示コンポーネントとして追加してください。データの取得・承認/差し戻しのロジックには一切触れないこと。

あわせて参考になる点:
- ステータスバッジをタイトル文字列のすぐ後ろにインラインで配置する見せ方(「マニュアル名【承認待ち】」のような形)も確認できました。現状の `承認待ち`/`差し戻し済み`/`公開済み` バッジは既にブリーフの配色規約(amber/red/green)に沿っているので、色は変更不要です。配置の見せ方だけ参考にしてよい。
- 一覧のソート可能な列ヘッダー(列名+↑↓アイコン)というパターンも見られましたが、これは並び替えクエリの追加が必要になるため、下記C-4のスコープ外リストに含めています。

### C-4. 今回はスコープ外(新機能・要検討)

以下はTeach me bizの実際の画面から見えた機能ですが、新しいテーブル・カラム・クエリ・サーバーアクションが必要になるため、今回のUI/UX限定パスの対象外とします。表示ロジックの変更だけでは実現できません:

- レシピ一覧のサムネイル画像表示(現状レシピにサムネイル用の画像データがあるか要確認)
- 一覧の列ソート機能(閲覧数・更新日などクリックで並び替え)
- 一覧のチェックボックス一括選択+移動/複製/アーカイブ/削除
- フォルダ単位の閲覧者/編集者権限設定・PDF出力可否設定
- マニュアル評価(グッジョブ)機能・評価CSV出力
- カミナシについては、クライアントいわく「UIそのものはあまり参考にしておらず、システムの概要を参考にした程度」とのことで、動画リンクの共有はあったものの画像取得ができないため、今回のパスではB-4の扱い(未着手)のまま変更しません。

これらは将来的に別途、機能追加として計画します。今回のパスでは触れないでください。

---

## 進め方の指示(第2弾を踏襲)

- A→B→Cの順、各セクション内は番号順に、1項目ずつ変更 → `npm run build` で確認 → 次の項目、と進めてください。
- 各修正後、実際にブラウザ(`npm run dev`)で管理者権限のあるアカウントを使って確認してください。
- 迷った場合は `CURSOR_BRIEF.md`(第2弾)に記載の配色・コンテナ幅・共有コンポーネントの規約を踏襲してください。
- 上記に明記した項目以外(特に「D. 今回はスコープ外」「B-4」「C-3」)には手を広げないでください。
