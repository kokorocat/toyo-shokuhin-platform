# Cursor プロンプト: UI/UX改善タスク(第2弾)

以下をそのままCursorのチャットに貼り付けてください。

---

## プロジェクト概要

日本の食品会社グループ(東洋食品/昭和食品/大阪惣菜、3社共同利用)向けの社内業務システム。Next.js 16 (App Router, TypeScript) + Supabase (PostgreSQL, Auth, RLS) + Tailwind CSS v4 + Vercelでホスティングされている。実際に稼働中の本番環境で、リアルな従業員データ(3,151名)・店舗データ(323店舗)が入っている。

リポジトリ: このディレクトリ全体(Next.jsプロジェクトルート)

**これは2回目のUI/UX改善パスです。** 1回目でポータルトップとHACCP店舗側画面(`/`, `/notices`, `/manuals`, `/haccp`, `/haccp/keypoint`, `/haccp/employee`, `/haccp/inspection`, `/haccp/holiday`, `/haccp/history`)は既に改善済みで、そこで作られた `src/components/Banner.tsx` / `src/components/PageHeader.tsx` / `src/components/EmptyState.tsx` が共有コンポーネントとして定着しています。**今回のスコープは、1回目の後に追加された5システム分の画面群**です。機能重視で高速に作られたため、デザインの作り込みが1回目の画面群と比べて手薄です。既存の共有コンポーネント・配色・レイアウト規約を踏襲しつつ、これらの新しい画面群の視覚的な質を1回目と同水準まで引き上げてください。

## 今回のタスクの範囲(重要)

**バックエンド・データ層・ビジネスロジックは一切変更しないでください。** 今回はUI/UXの改善のみが目的です。具体的には触ってはいけないもの:

- `supabase/migrations/` 配下のSQL(スキーマ・RLSポリシー)
- 各 `actions.ts` / `route.ts` 内のデータ検証・DB操作ロジック(バリデーション条件、フォームの `name` 属性、insert/update先のテーブル・カラム、注文ステータスの遷移可否判定等は変更不可。ボタンの見た目や配置、フォームのレイアウトはOK)
- `src/lib/portal/get-portal-context.ts`、`src/lib/supabase/`、`src/lib/haccp/admin-dashboard.ts`、`src/lib/date.ts` などの認証・権限・集計ロジック
- 各 `guard.ts`(`isHaccpAdminRole` 等の権限判定関数)

特に注意: `src/lib/date.ts` の `todayInTokyo()` は、以前 `new Date().toISOString().slice(0,10)` を各所で使っていたことでUTC基準の日付になり、日本時間の深夜0時〜9時台に日付がずれるバグが実際に発生し、全箇所を修正した経緯があります。UI側で日付のデフォルト値を扱うコードに触れる場合、**絶対に `new Date().toISOString()` 系のパターンを新規に持ち込まないでください**。既存の `todayInTokyo()` をそのまま使うか、触らないでください。

変更してよいもの: 見た目(Tailwindクラス、レイアウト、コンポーネント構成)、UIの状態管理(ローディング表示等)、アクセシビリティ、レスポンシブ対応。フォームの `<label>` テキストや説明文言の微調整もOKですが、`name` 属性(サーバーアクションが `formData.get("xxx")` で読む値)は絶対に変更しないこと。

## 今回のスコープ: 新しい画面群(5システム分)

### HACCP管理者機能(店舗側は改善済み、管理者側が対象)
- `/haccp/admin` — 回答状況ダッシュボード(重要ポイント/従業員衛生/自主点検/責任者確認の4セクション、絞り込みフォーム)
- `/haccp/admin/stores` — 店舗別回答状況一覧(テーブル、ページング)
- `/haccp/admin/stores/[storeId]` — 店舗別詳細(回答本文・訂正履歴・監査ログ、情報量が多い)
- CSV出力は画面を持たないため対象外

### 販促物受発注システム(新規システム全体)
- `/ordering` — 商品カタログ(1行1商品リスト、カテゴリチップ、検索)
- `/ordering/cart` — カート
- `/ordering/confirm` — 発注確認(クライアントコンポーネント)
- `/ordering/history` — 発注履歴
- `/ordering/admin` — 管理ダッシュボード
- `/ordering/admin/orders` — 受注一覧
- `/ordering/admin/orders/[orderId]` — 受注詳細・ステータス更新(条件付きフィールドが多く、UIが複雑になりがち)
- `/ordering/admin/products` — 商品一覧
- `/ordering/admin/products/new`, `/ordering/admin/products/[productId]` — 商品追加・編集フォーム

### レシピ閲覧システム(新規システム全体)
- `/recipe` — レシピ検索・一覧
- `/recipe/[id]` — レシピ詳細(原本ファイル、関連資料、関連商品)
- `/recipe/admin/submit` — 新規レシピ申請(ファイルアップロードあり)
- `/recipe/admin/approvals` — 承認待ち一覧

### 人事労務管理システム(新規システム全体)
- `/hr` — ダッシュボード+業務モジュール一覧(多くのカードが「未実装」表示で無効化されている想定通りの状態)
- `/hr/employees` — 社員一覧
- `/hr/employees/[personId]` — 社員詳細(雇用履歴・住所履歴・所属履歴など情報密度が高い)

## 既存の設計コンベンション(踏襲すること)

- ページは全てServer Component。データ取得はページ内で `Promise.all` により並列実行(直列awaitは過去に実際のパフォーマンス問題として指摘され修正済みなので、新規に直列化を持ち込まないこと)。
- フォーム送信は `<form action={サーバーアクション}>` のNext.js Server Actionsパターン。店舗側の画面(`/haccp/*`)は `SubmitButton`(useFormStatusでpending状態を表示)を使用。**今回対象の管理者系画面の一部にはまだpending状態表示がないものがあるため、追加を歓迎します**(ただしフォームの構造・action props は変更しないこと)。
- 配色: 基調はnavy/blue(`bg-blue-800`のボタン、`border-slate-200`のカード、`bg-slate-50`背景)。成功は緑、エラー・警告は赤、注意喚起はamber。バッジは `inline-flex items-center rounded-md px-2.5 py-1 text-xs font-bold` + 色ペア(緑=成功/公開中、赤=要対応/エラー、amber=注意、slate=対象外/非表示)という規約がありますが、**今回対象の画面群は別々のエージェントが並行して実装したため、同じ意味の状態でも画面によって色や文言が微妙に異なっている箇所があります**(例: 「承認待ち」「未確認」のようなニュートラル〜注意状態の色がページごとに揺れている)。こうした表記ゆれを横断的に見つけて統一してください。
- コンテナ幅: 店舗側の入力・一覧画面は `max-w-2xl`、管理者系のダッシュボード・テーブル画面は `max-w-5xl`。この使い分けは概ね守られていますが、統一感を確認してください。
- 共有コンポーネント(`src/components/Banner.tsx`, `PageHeader.tsx`, `EmptyState.tsx`)は今回対象の画面でも使われていますが、使用箇所・使用頻度にばらつきがあります。積極的に活用を広げてください。

## 既知の具体的な課題(優先的に着手してほしいもの)

1. **管理者系画面(`/haccp/admin/*`, `/ordering/admin/*`, `/recipe/admin/*`, `/hr/*`)は機能実装を優先して作られており、店舗側画面と比べて視覚的な作り込みが浅い。** 特にテーブル(受注一覧、店舗一覧、社員一覧、商品一覧)は素朴な実装になりがちなので、行のホバー状態・ゼブラストライプ・列幅の調整等でプロダクトらしい質感に引き上げてほしい。
2. **情報密度が高い詳細画面が読みにくい可能性がある**: `/haccp/admin/stores/[storeId]`(3帳票+訂正履歴+監査ログ)、`/hr/employees/[personId]`(雇用履歴×複数+住所履歴+所属履歴)、`/ordering/admin/orders/[orderId]`(明細+ステータス変更フォームの条件付きフィールド)。セクション分け・折りたたみ・視線誘導を工夫してほしい。
3. **モバイル対応**: 店舗側の新規画面(`/ordering`, `/ordering/cart`, `/ordering/confirm`, `/recipe`, `/recipe/[id]`)は店舗スタッフがスマートフォン・タブレットで使う前提。特にカートの数量増減ボタンや、商品タイプ別の追加入力フィールド(価格入POP・バイキングプライス)がタップしづらくないか確認してほしい。管理者系画面はPC中心でよいが、最低限レイアウト崩れがないこと。
4. **状態バッジの表記・色の統一**(上記コンベーション欄参照)。
5. **フォームのpending状態表示**が店舗側ほど徹底されていない管理者系フォーム(商品追加・編集、受注ステータス更新、レシピ申請、承認・差し戻し)への追加。
6. **HR画面の「未実装」カード**(`/hr` の業務モジュール一覧)は意図的に無効化されたデザインですが、視覚的にもう少し洗練させても良い(現状は簡素なグレーカード)。

## 進め方の指示

- 一度に全画面を書き換えず、1画面ずつ変更→ `npm run build` で確認→次の画面、という進め方をしてほしい。
- 各画面を変更したら、実際にブラウザ(`npm run dev`)で開いて、レスポンシブ表示(特にスマートフォン幅)を確認してから次に進んでほしい。管理者系ログイン(会社管理者または全権限管理者ロール)でないと `/haccp/admin/*`, `/ordering/admin/*`, `/recipe/admin/*`, `/hr/*` は「権限がありません」表示になるため、権限のあるアカウントで確認すること。
- 迷った場合は、派手なリデザインよりも「既存の情報構造・機能を崩さず、視覚的な質を上げる」ことを優先してほしい。
- 優先順位に迷ったら、上記「既知の具体的な課題」の番号順に着手してほしい。

作業を始める前に、`src/app/` 配下の対象ディレクトリ(`haccp/admin`, `ordering`, `recipe`, `hr`)を一通り読んで現状の実装を把握し、あわせて `src/components/` の共有コンポーネントの中身も確認してから着手してください。
