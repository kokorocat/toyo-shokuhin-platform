# Cursor プロンプト: UI/UX改善タスク

以下をそのままCursorのチャットに貼り付けてください。

---

## プロジェクト概要

日本の食品会社グループ(東洋食品/昭和食品/大阪惣菜、3社共同利用)向けの社内業務システム。Next.js 16 (App Router, TypeScript) + Supabase (PostgreSQL, Auth, RLS) + Tailwind CSS v4 + Vercelでホスティングされている。実際に稼働中の本番環境で、リアルな従業員データ(3,151名)・店舗データ(323店舗)が入っている。

リポジトリ: このディレクトリ全体(Next.jsプロジェクトルート)

## 今回のタスクの範囲(重要)

**バックエンド・データ層・ビジネスロジックは一切変更しないでください。** 今回はUI/UXの改善のみが目的です。具体的には触ってはいけないもの:
- `supabase/migrations/` 配下のSQL(スキーマ・RLSポリシー)
- 各 `actions.ts` 内のデータ検証・DB操作ロジック(バリデーション条件やinsert/update先のテーブル・カラムは変更不可。ボタンの見た目や配置、フォームのレイアウトはOK)
- `src/lib/portal/get-portal-context.ts` や `src/lib/supabase/` 配下の認証・権限まわり

変更してよいもの: 見た目(Tailwindクラス、レイアウト、コンポーネント構成)、UIの状態管理(ローディング表示等)、アクセシビリティ、レスポンシブ対応。

## 現在の画面構成

- `/login` — ログイン画面
- `/` — ポータルトップ(会社・エリア・店舗情報表示、お知らせ/マニュアル/HACCP/販促物受発注/レシピ閲覧への導線)
- `/notices`, `/notices/[id]` — お知らせ一覧・詳細
- `/manuals` — マニュアル(現状プレースホルダーのみ)
- `/haccp` — HACCP管理トップ(本日の状況、責任者確認)
- `/haccp/keypoint` — 重要ポイント・温度・ラベル入力(日次)
- `/haccp/employee` — 従業員衛生入力(日次・従業員ごと)
- `/haccp/inspection` — 食品衛生自主点検入力(月次、17問)
- `/haccp/holiday` — 店休日登録
- `/haccp/history` — 過去回答一覧

## 既存の設計コンベンション(踏襲すること)

- ページは全てServer Component。データ取得はページ内で `Promise.all` により並列実行(直列awaitは過去に実際のパフォーマンス問題として指摘され修正済みなので、新規に直列化を持ち込まないこと)。
- フォーム送信は `<form action={サーバーアクション}>` のNext.js Server Actionsパターン。素の `<button type="submit">` ではなく、`src/app/haccp/SubmitButton.tsx` の `SubmitButton` コンポーネント(useFormStatusでpending状態を表示)を必ず使うこと。これも過去に「ボタンを押しても反応が分からない」という実際のクライアントからの指摘を受けて導入した経緯がある。
- 配色: 基調はnavy/blue(`bg-blue-800`のボタン、`border-slate-200`のカード、`bg-slate-50`背景)。成功は緑(`bg-green-50`/`text-green-700`)、エラー・警告は赤(`bg-red-50`/`text-red-700`)、注意喚起はamber。クライアントへ提出する仕様書等のドキュメントもnavy基調のデザインなので、そのブランドイメージとの一貫性を意識すること。
- コンテナ幅は基本 `max-w-2xl`(HACCPトップのカードグリッドのみ `max-w-4xl`)。

## 既知の具体的な課題(優先的に着手してほしいもの)

1. **全体的な視覚的な作り込みが不十分**。現状は機能を満たすだけの最小限のTailwindスタイリングで、クライアントから「クオリティが低い」という趣旨のフィードバックを受けている。カード・フォーム・ボタン・バッジ等のビジュアルデザインをプロダクトらしい水準に引き上げてほしい。
2. **モバイル最適化が不十分**。店舗側の利用者はスマートフォン・タブレットが主な利用端末という要件が仕様書で明記されている。特に:
   - `/haccp/keypoint` の温度・ラベル判定の `<select>` はタップしづらい可能性がある(`/haccp/employee` と `/haccp/inspection` の良好/異常選択は既に大きめのボタン形式に改善済み — このパターンを他の選択UIにも展開してほしい)
   - フォーム全体の余白・タップ領域を片手操作前提で見直してほしい
3. **お知らせ一覧・マニュアル画面の作り込みが薄い**。HACCP画面群と比べてビジュアルの一貫性が取れていない可能性がある。
4. **空状態・成功状態・エラー状態の表示に統一感がない**。ページごとにバラバラに実装されているので、共通コンポーネント化を検討してほしい(例: `<Banner variant="success|error|info">` のような共有コンポーネント)。
5. **ローディング表示**は `src/app/haccp/loading.tsx` (Next.jsのloading.tsx規約)を導入済みだが、`/notices` や `/manuals` 配下には無いので追加してほしい。

## 進め方の指示

- 一度に全画面を書き換えず、1画面ずつ変更→ `npm run build` で確認→次の画面、という進め方をしてほしい。
- 各画面を変更したら、実際にブラウザ(`npm run dev`)で開いて、レスポンシブ表示(特にスマートフォン幅)を確認してから次に進んでほしい。
- 迷った場合は、派手なリデザインよりも「既存の情報構造・機能を崩さず、視覚的な質を上げる」ことを優先してほしい。

作業を始める前に、`src/app/` 配下の全ページを一通り読んで、現状の実装を把握してから着手してください。
